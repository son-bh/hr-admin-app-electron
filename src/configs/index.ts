import { PATH_NAME } from './pathName';
import { USER_ROLE, USER_TEAM } from './userRole';

export const LANGUAGE = {
  ENGLISH: 'en',
  VIETNAMESE: 'vn',
};

export { PATH_NAME, USER_ROLE, USER_TEAM };
